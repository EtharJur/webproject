<%- include('./Layouts/header.ejs') -%>

<section>
    <% salads.forEach(function(salad) { %> 
        <div class="recipe">
            <h2><%= salad.name %></h2>
            <img src="<%= salad.image %>" alt="<%= salad.name %>">
            <p><%= salad.description %></p>

            <h3>Ingredients:</h3>
            <ul>
                <% salad.ingredients.forEach(function(ingredient) { %>
                    <li><%= ingredient %></li>
                <% }); %>
            </ul>

            <h3>Instructions:</h3>
            <ol>
                <% salad.recipe.forEach(function(instruction) { %>
                    <li><%= instruction %></li>
                <% }); %>
            </ol>
        </div>
    <% }); %>
</section>

<div class="pagination">
    <% if (current > 1) { %>
        <a href="/salads?page=<%= current - 1 %>" class="prev">Previous</a>
    <% } %>

    <% for (let i = 1; i <= pages; i++) { %>
        <a href="/salads?page=<%= i %>" class="<%= (i == current) ? 'active' : '' %>">
            <%= i %>
        </a>
    <% } %>

    <% if (current < pages) { %>
        <a href="/salads?page=<%= current + 1 %>" class="next">Next</a>
    <% } %>
</div>

<%- include('./Layouts/footer.ejs') -%>